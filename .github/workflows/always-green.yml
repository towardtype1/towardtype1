name: CI

on:
  schedule:
    - cron:  '0 * * * *'

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: |
          git config user.name towardtype1
          git config user.email edd@towardtype1.com
          git add .
          git commit --allow-empty -m "Always green!"
          git push
